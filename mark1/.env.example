# Set these in your terminal environment or Railway Variables. Never commit a real key.
DATA_DIR=/data
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
# After Railway generates the domain, use its exact HTTPS origin (no trailing slash).
PUBLIC_ORIGIN=
